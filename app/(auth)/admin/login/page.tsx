'use client';
import Link from 'next/link'; import { FormEvent, useState } from 'react'; import { loginOwner, ownerExists } from '@/components/adminBrowserAuth';
export default function LoginAdmin() { const [error, setError] = useState(''); const submit = async (event: FormEvent<HTMLFormElement>) => { event.preventDefault(); const form = new FormData(event.currentTarget); try { await loginOwner(String(form.get('email')), String(form.get('password'))); window.location.assign('/admin/dashboard'); } catch (err) { setError(err instanceof Error ? err.message : 'Sign in failed.'); } }; return <main className="vivo-public-shell grid min-h-screen place-items-center p-4 text-white"><form onSubmit={submit} className="vivo-glass-dialog w-full max-w-md rounded-xl p-7"><p className="text-xs font-bold tracking-[.16em] text-[#FF6A00]">VIVO AMIGO</p><h1 className="mt-2 text-3xl font-extrabold">Super Admin sign in</h1>{error && <p role="alert" className="mt-4 text-sm text-red-300">{error}</p>}<label className="mt-6 block text-xs font-bold">Email<input name="email" type="email" required className="mt-2 w-full rounded-lg border border-white/10 bg-black/20 p-3" /></label><label className="mt-4 block text-xs font-bold">Password<input name="password" type="password" required className="mt-2 w-full rounded-lg border border-white/10 bg-black/20 p-3" /></label><button className="mt-6 w-full rounded-lg bg-[#FF6A00] p-3 font-bold">Sign in</button>{!ownerExists() && <Link href="/admin/register" className="mt-4 block text-center text-sm text-[#FFB38A]">Create your Super Admin account</Link>}</form></main>; }
