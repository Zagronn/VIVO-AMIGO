{
  "name": "vivo-amigo-mobile",
  "version": "0.1.0",
  "private": true,
  "description": "Shared React Native architecture for PAY VIVO, CARGO VIVO, and VIVO POS",
  "scripts": {
    "check": "node --check src/index.js && node --check src/api.js && node --check src/offlineQueue.js && node --check src/apps.js && node --check src/qr.js && node --check src/biometrics.js",
    "check:host": "test -f App.js && test -f app.json"
  },
  "dependencies": {
    "@react-native-async-storage/async-storage": "^2.2.0",
    "react": "^19.1.0",
    "react-native": "^0.81.4",
    "react-native-biometrics": "^3.0.1",
    "react-native-vision-camera": "^4.7.2"
  }
}
